version: '3'

services:
  step-ca:
    container_name: step-ca
    image: smallstep/step-ca
    restart: unless-stopped
    network_mode: host
    ports:
      - 9000:9000
    volumes:
      - /containers/step-ca:/home/step
